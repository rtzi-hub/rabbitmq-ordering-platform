apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: logging

resources:
  - kibana-import-job.yaml

configMapGenerator:
  - name: kibana-saved-objects
    files:
      - kibana-dashboards.ndjson=kibana-dashboards.ndjson
generatorOptions:
  disableNameSuffixHash: true
