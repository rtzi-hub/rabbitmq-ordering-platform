apiVersion: v1
kind: Secret
metadata:
  name: postgresql-credentials
  namespace: apps
type: Opaque
stringData:
  # admin user = postgres
  postgres-username: "postgresdb"
  postgres-password: "postgresqlpassword"
  # app user = postgresdb
  password: "postgresqlpassword"
